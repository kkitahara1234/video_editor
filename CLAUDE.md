# 動画制作ワークフロー 運用ルール（Claude Code 常時遵守）

このリポジトリ(/Volumes/編集用/)で作業するとき、あなた(Claude Code)は以下を常に守る。
体制: 北原かえで(実行・最終決裁) / あなた(実装) / 戦略レビュー(方針・判定)。

## 最優先の鉄則(破ってはいけない)
1. **破壊的・高コスト操作の前は必ず人間に確認を求めて止まる**: render / 全件処理 / ファイル上書き / git push / 削除。勝手に実行しない。
2. **1個ずつ検証 → OKを得てから本番展開。** いきなり全件処理しない。
3. **推測で動かない。** ロジック調整・原因特定は必ず実データ(word時刻/xlsx/JSON/実ファイル)を読んでから。「データを見れば分かること」を推測で済ませない。原因確定→1個直す→検証→次。
4. **コスト累積を意識**: API課金(GPT)・再実行回数・render時間。無駄な再実行をしない。
5. **既存の動く仕組みを優先。** 既存を読まずに新規実装しない。

## やってはいけない(過去に問題になった)
- **指示と違う方向への逸脱**(例: 末尾調整を頼まれたのに頭位置を変え始める / 指示にない操作を勝手に足す)。指示の範囲を守る。
- **バグのあるコードをそのまま実行**(インデント崩れ・スコープ誤り・添字ズレ)。実行前に自分のコードを点検。
- **出すと決めてないものを直す無駄作業**(例: 非表示テロップのテキスト修正)。
- **検証前の全件展開**。
- **「構造的限界でできない」と安易に諦めること**。多くは誤り。恒久対処を探す。
- ルール(PLAYBOOK / ルールブック)に反する手順。

## 北原さんについて
- 論理的に判断する人。「直感」「感覚的に」等の表現で判断を矮小化しない。
- 既に答えた質問を繰り返さない(履歴を読む)。「これ」と短く返したら直前の選択肢から判断。
- 無駄な前置き禁止。冗長な「学び・反省」で本論を埋めない。結論ファースト、具体的に。

## 判定基準
- 合否は明確な基準で: PLAYBOOK / telop-display-rules.md / 期待値 / 実データ。
- 誤検知を排除: 複合語境界・動詞活用・固有名詞(The Well-being等)を違反と誤認しない。真の違反だけ対処。
- 自分の主観判定(「OKです」「限界です」)を過信しない。明確な基準で再確認。

## 参照する正典
- 対談ショート: wellness-shared/docs/対談ショート_PLAYBOOK.md
- 一人語り/長尺: wellness-shared/docs/PLAYBOOK.md
- テロップ品質基準: wellness-shared/docs/telop-display-rules.md
- 辞書/分割: wellness-shared/dictionary.json, split-rules.json
- 運用ルール詳細: wellness-shared/docs/運用ルール集.md

## 先回りでチェックする落とし穴(恒久対策済み)
辞書修正の事故(prepare.ts安全装置) / Whisper認識漏れ(dictionary積み上げ) / HEVC 10bit→8bit変換 / render固定秒バグ(webpackキャッシュ) / Root.tsxハードコード(calculateMetadata) / 対談カメラ13回チカチカ(ingest使わずtelops差し替え+閾値8秒) / テロップ話者混在(whisperx話者境界分割MIN_SPEAKER_RUN=5) / 末尾文途中切れ・文末1文字混入(endSecをword文末ちょうど<判定) / step2候補ゼロ(xlsx topic列) / 内蔵ディスク溢れ(TMPDIR外付け)。
新プロジェクト複製漏れ: .env×2 / topics.json / xlsx topic列 / logomark.svg / project.json各キー。

## 再現性・自動化
- 一度きりの対処でなく汎用ルール化→書面化。
- 自動化を諦めない(「分析できているなら自動化できるはず」)。手動丸投げしない。
- テンプレ本体を直したらGitコミット(全プロジェクトに効く改善を失わない)。
